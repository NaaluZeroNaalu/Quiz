<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
    <title>Document</title>
</head>
<body>

    <style>
        * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", "Roboto", "Oxygen", "Ubuntu", "Cantarell", "Fira Sans", "Droid Sans", "Helvetica Neue", sans-serif;
    background-color: #fafafa;
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh;
}

.login-container {
    display: flex;
    justify-content: center;
    align-items: center;
    width: 100%;
    height: 100%;
}

.login-box {
    background-color: white;
    padding: 40px;
    border-radius: 4px;
    box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    text-align: center;
    width: 350px;
}

.logo img {
    width: 175px;
    margin-bottom: 30px;
}

.login-form input {
    width: 100%;
    padding: 12px;
    margin-bottom: 10px;
    border: 1px solid #dbdbdb;
    border-radius: 3px;
    font-size: 14px;
}

.login-btn {
    width: 100%;
    padding: 12px;
    background-color: #0095f6;
    color: white;
    font-size: 14px;
    border: none;
    border-radius: 3px;
    cursor: pointer;
    font-weight: 600;
}

.login-btn:hover {
    background-color: #0078d4;
}

.divider {
    margin: 15px 0;
    font-size: 14px;
    color: #999;
}

.divider span {
    background-color: #fff;
    padding: 0 10px;
}

.fb-login-btn {
    width: 100%;
    padding: 12px;
    background-color: #1877f2;
    color: white;
    font-size: 14px;
    border: none;
    border-radius: 3px;
    cursor: pointer;
    font-weight: 600;
    display: flex;
    justify-content: center;
    align-items: center;
}

.fb-login-btn img {
    width: 18px;
    margin-right: 8px;
}

.forgot-password a,
.sign-up a {
    text-decoration: none;
    color: #0095f6;
    font-size: 14px;
}

.forgot-password {
    margin-top: 15px;
}

.sign-up {
    margin-top: 20px;
    font-size: 14px;
}

.sign-up a {
    font-weight: bold;
}

    </style>
    <div class="login-container">
        <div class="login-box">
        

            <div  class="login-form">
                <input type="text" placeholder="Your name" id="name"  required>
                <input type="date" placeholder="Password" id="dob" required>
                <input type="text" placeholder="Who you are to ajith" id="relation" required>
                <p>Dont worry name only shown to Ajith</p>
                <p id="message" style="color: red;"></p>
                <br>
                <br>
                <button onclick="Check()" class="login-btn" style="background-color: black;">Start The Quiz</button>
            </div>

            <div class="divider">
                <span>OR</span>
            </div>

            <p> </p>
            <div class="forgot-password">
                <a href="/">To see others scores? Login With Instagram</a>
            </div>

          
            <script>
                function Check(){

                    let name = document.getElementById("name").value
                    let dob = document.getElementById("dob").value
                    let relation = document.getElementById("relation").value

                    if(name.length === 0 || dob.length === 0 || relation.length === 0){

                        document.getElementById("message").innerHTML = "please first enter the details"
                    }else{
                        
                        document.getElementById("message").innerHTML = ""
                        window.localStorage.setItem("user",name)
                        window.localStorage.setItem("role","guest")
                        $.ajax({
            url:'http://localhost:3000/storeguest',
            method:'POST',
            data:{
                name:$("#name").val(),
                dob:$("#dob").val(),
                relation:$("#relation").val(),
     
            },
            success:function(response){
                console.log("ok")
                window.location.href = "http://localhost:3000/quiz"
            },
            error:function (err) {
                console.log(err)
            }
     })
                    }
                }
            </script>
        </div>
    </div>
    
</body>
</html>